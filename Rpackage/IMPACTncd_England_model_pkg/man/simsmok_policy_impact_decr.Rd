% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{simsmok_policy_impact_decr}
\alias{simsmok_policy_impact_decr}
\title{Simulate policy impact when smoking prevalence DECREASES (structural changes)}
\usage{
simsmok_policy_impact_decr(
  smok_status,
  smok_quit_yrs,
  smok_dur,
  smok_cig,
  new_pid,
  hc_eff
)
}
\arguments{
\item{smok_status}{Smoking status vector (input should have status = 4 where hc_eff = 1)}

\item{smok_quit_yrs}{Years since quitting}

\item{smok_dur}{Years of smoking}

\item{smok_cig}{Cigarettes per day}

\item{new_pid}{Logical vector marking first year of each person}

\item{hc_eff}{Policy effect indicator (1=convert to ex-smoker, 0=no effect)}
}
\value{
List with modified smok_status, smok_quit_yrs, smok_dur, smok_cig
}
\description{
Used for scenarios where smoking is decreasing (e.g., tobacco control policies,
price increases, smoking bans). Current smokers (status = 4) are converted to
ex-smokers (status = 3) based on hc_eff indicator.
}
\details{
LOGIC:
A boolean 'marker' tracks individuals affected by the policy:

For new persons (new_pid = TRUE):
\enumerate{
\item If hc_eff = 1:
\itemize{
\item Assumes input status = 4 (current smoker, ensured in R side)
\item Set marker = TRUE
\item Change status to 3 (ex-smoker)
\item Set quit_yrs = 1
\item Set duration = input duration - 1
\item NOTE: Prevents illegal transition 1 -> 3 (never -> ex without being current)
}
}

For continuing persons (not new_pid):
\enumerate{
\item If hc_eff = 1:
\itemize{
\item Set marker = TRUE
\item If previous status != 1 (not never smoker):
\itemize{
\item Change status to 3, quit_yrs = 1, duration = input - 1
}
\item If previous status = 1 (never smoker):
\itemize{
\item Keep status = 1, reset all smoking variables to 0
\item Prevents illegal 1 -> 3 transition
}
}
\item If marker = TRUE and hc_eff = 0 (years following policy effect):
\itemize{
\item If previous status = 1: maintain status 1, all variables = 0
\item If status = 4: maintain quit_yrs = 0, increment duration
\item If status = 3: increment quit_yrs, carry forward duration
}
\item Reset marker = FALSE when encountering new person
}
}
