% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ZenodoManager_class.R
\name{ZenodoAssetManager}
\alias{ZenodoAssetManager}
\alias{.zenodo_archive_worker}
\title{R6 Class for managing Zenodo data archives}
\usage{
.zenodo_archive_worker(task)
}
\arguments{
\item{hash_file}{Path to store/read hash manifest (default: "./simulation/zenodo_manifest.csv").}

\item{archive_dir}{Temporary directory for archives (default: tempdir()).}

\item{logs}{Enable verbose logging.}

\item{token}{Zenodo personal access token. If NULL, reads from ZENODO_TOKEN env var.}

\item{sandbox}{Use Zenodo sandbox for testing.}

\item{upload}{Callback for upload progress. Signature: function(bytes, total, filename).
Set to NULL to disable, or TRUE for a default console progress bar.}

\item{download}{Callback for download progress. Signature: function(bytes, total, filename).
Set to NULL to disable, or TRUE for a default console progress bar.}

\item{concept_doi}{Optional concept DOI.}

\item{input_dirs}{Character vector of full paths to directories to archive.}

\item{archive_name}{Name of the archive file (without extension).}

\item{include_patterns}{File patterns to include (default: all files).}

\item{exclude_patterns}{Regex patterns to exclude directories/subfolders.}

\item{group_by_prefix}{If TRUE, groups subfolders within each directory by prefix
(everything before the last underscore) into separate archives.}

\item{compression_level}{Integer 0-9 passed to \code{zip::zip()}.
0 = no compression (fastest), 9 = maximum compression (smallest files).
Default: 6.}

\item{multicore}{If TRUE, create archives in parallel across directories
using PSOCK clusters (parallelly/parallel) on all platforms.}

\item{n_cores}{Number of cores for parallel execution
(default: \code{parallel::detectCores(logical = FALSE)}).
Capped to the number of archive tasks.}

\item{update_gitignore}{If TRUE, automatically adds archived source files
to .gitignore (at the git repository root) unless they are already
covered by an existing pattern. Uses efficient directory/extension
patterns where possible (e.g., \code{inputs/mortality/*.fst}).
Also removes any already-tracked files from the git index
(\code{git rm --cached}) so they are no longer committed in future
pushes. The files are kept on disk — only the git tracking is removed.
Requires git to be installed and the project to be in a git repository.}

\item{input_dir}{Directory to scan.}

\item{recursive}{Scan recursively.}

\item{file_patterns}{File patterns to include.}

\item{manifest}{A data.table manifest.}

\item{path}{Path to load (default: self$hash_file).}

\item{local_manifest}{Local file manifest.}

\item{remote_manifest}{Remote file manifest.}

\item{exclude_file_patterns}{Regex patterns to exclude files (should match what was used during upload).}

\item{record}{Zenodo record object. If NULL, uses self$record.}

\item{filename}{Name of the file to download.}

\item{archive_path}{Path to the zip archive.}

\item{dest_dir}{Destination directory.}

\item{title}{Record title.}

\item{description}{Record description.}

\item{creators}{List of creators (each with firstname, lastname, optional orcid).}

\item{keywords}{Vector of keywords.}

\item{license}{License identifier (default: "cc-by-4.0").}

\item{publisher}{Publisher name (required by InvenioRDM for DOI
registration). Defaults to "Zenodo".}

\item{delete_previous_files}{Delete files from previous version.}

\item{input_base}{Base input directory.}

\item{directories}{Directories to sync (NULL for all).}

\item{action}{Action to perform: "check", "download", "upload".}

\item{overwrite}{Overwrite local files when downloading.}

\item{version}{Version for new uploads.}

\item{inputs_manifest}{An \code{InputsManifest} object with the current
manifest loaded and \code{zenodo_managed} flags set via
\code{InputsManifest$set_zenodo_dirs()}.}

\item{manifest_path}{Character. Path to the current manifest CSV
(default: \code{"./simulation/inputs_manifest.csv"}).}
}
\value{
A new \code{ZenodoAssetManager} object.

The invisible self for chaining.

The invisible self for chaining.

The invisible self for chaining.

The Zenodo record object.

A data.table with file information (filename, size, checksum).

A list with archive_path, source_hash, and file_count.

A list with archive_path, source_hash, and file_count.

A data.table with archive information.

A data.table with file hashes.

The invisible self for chaining.

A data.table manifest or NULL if file doesn't exist.

A list with added, removed, and modified files.

A data.table showing which archives have changed.

The invisible self for chaining.

Path to downloaded file.

Path to extracted directory.

The created record object.

The new version record object.

The published record object.

A data.frame with version information.

Sync status information.

A named list with character vectors \code{added}, \code{removed},
\code{modified}, \code{unchanged} (relative paths). Returns \code{NULL} if no
upload snapshot exists yet (i.e., no prior upload has been done).

Invisible self (for method chaining).
}
\description{
The \code{ZenodoAssetManager} class provides a complete pipeline for managing
input data assets via Zenodo. It supports uploading, downloading, hashing,
and versioning of parquet and other data files.

Create a new ZenodoAssetManager object.

Connect to Zenodo API.

Set the concept DOI for the record.

Set progress callback functions for uploads and/or downloads.

Get the Zenodo record by concept DOI.
Searches published records first, then falls back to draft records.

List files in the current Zenodo record.

Create a zip archive from a directory.

Create a zip archive from multiple directories.

Create zip archives for input directories.

Compute hash manifest for a directory.

Save manifest to file.

Load manifest from file.

Compare two manifests to find differences.

Check if local source files have changed since last upload.

Upload an archive to Zenodo.

Download a file from Zenodo record.

Extract a zip archive to a directory.

Create a new Zenodo record (deposit).

Create a new version of an existing record.

Publish the current record.

Get all versions of the current record.

Synchronize local inputs with Zenodo.

Print summary of the manager.

Compare the current \code{InputsManifest} against the
snapshot taken at the last Zenodo upload to detect local changes.

Only Zenodo-managed files are compared (those with
\code{zenodo_managed == TRUE}). This allows you to see which input files
have changed since the last Zenodo upload and decide whether a new
Zenodo version is needed.

The upload snapshot is saved by \code{snapshot_manifest_for_upload()} and
stored at \code{simulation/inputs_manifest_at_last_upload.csv}.

Save a copy of the current inputs manifest to record
the state at the time of upload.

Called automatically by \code{zenodo_upload_inputs()}. The snapshot is stored at
\code{simulation/inputs_manifest_at_last_upload.csv} and is used by
\code{compare_with_inputs_manifest()} to detect subsequent local changes.
}
\details{
This class wraps the zen4R package to provide:
\itemize{
\item \strong{Archive Creation}: Groups files into zip archives by directory
\item \strong{Hash Verification}: Uses digest to detect file changes
\item \strong{Version Control}: Manages DOI-based versioning on Zenodo
\item \strong{Cross-Platform}: Works on Linux, Windows, and macOS
}
}
\section{Fields}{

\describe{
\item{\code{zenodo}}{A ZenodoManager object from zen4R package.}

\item{\code{concept_doi}}{The concept DOI for the record (shared across versions).}

\item{\code{record}}{The current Zenodo record object.}

\item{\code{hash_file}}{Path to the hash manifest file.}

\item{\code{archive_dir}}{Directory for storing temporary archives.}

\item{\code{logs}}{Enable verbose logging.}

\item{\code{sandbox}}{Use Zenodo sandbox for testing.}

\item{\code{upload_progress}}{Callback function for upload progress.
Signature: function(bytes_transferred, total_bytes, filename)}

\item{\code{download_progress}}{Callback function for download progress.
Signature: function(bytes_transferred, total_bytes, filename)}
}}

\section{Zenodo Authentication}{

Requires a Zenodo personal access token. Set via:
\itemize{
\item Environment variable: \code{ZENODO_TOKEN}
\item Or pass directly to \code{connect()} method
}

Get your token at: https://zenodo.org/account/settings/applications/tokens/new/
}

\examples{
\dontrun{
# Use default console progress bars
manager$set_progress_callback(upload = TRUE, download = TRUE)

# Use custom callback
manager$set_progress_callback(
  upload = function(bytes, total, filename) {
    pct <- round(100 * bytes / total, 1)
    cat(sprintf("\r\%s: \%.1f\%\%", filename, pct))
  }
)

# Disable progress callbacks
manager$set_progress_callback(upload = NULL, download = NULL)
}
\dontrun{
# Archive specific directories
manager$create_input_archives(directories = c("mortality", "pop"))

# Exclude certain patterns
manager$create_input_archives(exclude_patterns = c("^old_", "_backup$"))

# Group subfolders within each directory by prefix
# e.g., disease_burden/cancer_2020, disease_burden/cancer_2021 -> disease_burden_cancer.zip
manager$create_input_archives(group_by_prefix = TRUE)

# Exclude R files (default) and other patterns
manager$create_input_archives(exclude_file_patterns = c("\\\\.R$", "\\\\.Rmd$"))

# Parallel archive creation
manager$create_input_archives(multicore = TRUE, n_cores = 4L)

# Auto-update .gitignore with archived files
manager$create_input_archives(update_gitignore = TRUE)
}
\dontrun{
changes <- manager$check_source_changes("./inputs")
# Only re-archive and upload changed directories
changed_dirs <- changes[changed == TRUE, directory]
}
}
