% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InputsManifest_class.R
\name{.manifest_hash_worker}
\alias{.manifest_hash_worker}
\title{Hash a single file for the inputs manifest}
\usage{
.manifest_hash_worker(file_path, algo = "xxhash64")
}
\arguments{
\item{file_path}{Character. Absolute path to the file to hash.}

\item{algo}{Character. Hash algorithm passed to \code{\link[digest:digest]{digest::digest()}}
(default: \code{"xxhash64"}).}
}
\value{
A named list with four elements:
\describe{
\item{file_path}{The input path (unchanged).}
\item{hash}{The computed hash string.}
\item{size_bytes}{File size in bytes.}
\item{mtime}{File modification time as ISO 8601 string.}
}
}
\description{
Standalone worker function for parallel hashing of input files. Defined
outside the R6 class so it can be serialized to PSOCK cluster workers
without dragging along non-serializable R6 environments or HTTP connections.
}
\details{
This function follows the project convention for PSOCK-safe worker
functions: it receives only plain serializable data (character scalars) and
returns a plain list. See also \code{.zenodo_archive_worker()} in
\code{ZenodoManager_class.R} for the same pattern.
}
\seealso{
\code{InputsManifest} which calls this function via
\code{\link[parallel:parLapplyLB]{parallel::parLapplyLB()}}.
}
\keyword{internal}
